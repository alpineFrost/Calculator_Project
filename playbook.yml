- name: Pull and Run docker image
  hosts: host_machine
  tasks:
  
    - name: Start docker service
      service:
        name: docker
        state: started

    - name: Pull Image
      shell: docker pull abhigna4443/calculator_project:latest

    - name: Run the container
      shell: docker run -it -d abhigna4443/calculator_project
